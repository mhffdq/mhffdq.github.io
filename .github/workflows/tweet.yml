name: PostTweet

on:
  push:
    branches:
      - gh-pages
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: PostTweet
      run: curl -X POST -d value='tweet' ${{ secrets.WEBHOOKURL }}
