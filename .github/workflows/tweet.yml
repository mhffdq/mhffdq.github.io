name: PostTweet

on:
  push:
    branches:
      - gh-pages

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: PostTweet
      run: curl -X POST -d value='tweet' ${{ secrets.WEBHOOKURL }}
